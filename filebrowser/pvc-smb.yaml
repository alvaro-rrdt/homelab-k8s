apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filebrowser-smb-pvc
  namespace: filebrowser
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Ti
  storageClassName: ""
  volumeName: filebrowser-smb-pv
