apiVersion: v1
kind: PersistentVolume
metadata:
  name: actualbudget-smb-pv
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - vers=3.0
  csi:
    driver: smb.csi.k8s.io
    readOnly: false
    volumeHandle: actualbudget-smb-pv
    volumeAttributes:
      source: "//u648454.your-storagebox.de/backup/backups/actualbudget"
    nodeStageSecretRef:
      name: immich-smb-creds
      namespace: immich
